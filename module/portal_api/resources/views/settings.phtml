<?php

declare(strict_types=1);

use Engelking\Webtrees\PortalApi\PortalApiModule;
use Fisharebest\Webtrees\Http\RequestHandlers\ControlPanel;
use Fisharebest\Webtrees\Http\RequestHandlers\ModulesAllPage;
use Fisharebest\Webtrees\I18N;
use Fisharebest\Webtrees\Tree;
use Illuminate\Support\Collection;

/**
 * @var Collection<int,Tree> $trees
 * @var PortalApiModule      $module
 * @var string               $title
 * @var string               $tree
 * @var string               $portal_url
 * @var string               $proxy_secret
 * @var string               $rate_limit_ip
 * @var string               $rate_limit_user
 * @var string               $rate_limit_window
 * @var string               $invitation_days
 * @var string               $invitations_url
 * @var string               $diagnosis_url
 * @var string               $member_invites
 * @var string               $member_invite_steps
 * @var string               $member_invite_quota
 * @var string               $member_path_length
 */

$tree_options = ['' => I18N::translate('Use the site’s default family tree')];

foreach ($trees as $a_tree) {
    $tree_options[$a_tree->name()] = $a_tree->title();
}

?>

<?= view('components/breadcrumbs', ['links' => [route(ControlPanel::class) => I18N::translate('Control panel'), route(ModulesAllPage::class) => I18N::translate('Modules'), $title]]) ?>

<h1><?= e($title) ?></h1>

<p>
    <?= I18N::translate('This module serves the member portal. Members can view the family tree and propose changes to their own record; those changes go to the pending changes list for approval.') ?>
</p>

<p>
    <a class="btn btn-outline-primary" href="<?= e($invitations_url) ?>">
        <?= I18N::translate('Invitations') ?>
    </a>
    <a class="btn btn-outline-primary" href="<?= e($diagnosis_url) ?>">
        <?= I18N::translate('Diagnosis') ?>
    </a>
</p>

<form method="post">
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_TREE ?>">
            <?= I18N::translate('Family tree') ?>
        </label>
        <div class="col-sm-9">
            <?= view('components/select', ['name' => PortalApiModule::SETTING_TREE, 'id' => PortalApiModule::SETTING_TREE, 'selected' => $tree, 'options' => $tree_options]) ?>
            <div class="form-text">
                <?= I18N::translate('The portal serves one family tree. Members see only records from this tree.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_PORTAL_URL ?>">
            <?= I18N::translate('Portal address') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="url" id="<?= PortalApiModule::SETTING_PORTAL_URL ?>" name="<?= PortalApiModule::SETTING_PORTAL_URL ?>" value="<?= e($portal_url) ?>" placeholder="https://portal.example.org">
            <div class="form-text">
                <?= I18N::translate('Where members reach the portal. Used for the link in password reset emails, which must point at the portal rather than at webtrees. Leave empty to switch password resets off.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_PROXY_SECRET ?>">
            <?= I18N::translate('Proxy secret') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="text" id="<?= PortalApiModule::SETTING_PROXY_SECRET ?>" name="<?= PortalApiModule::SETTING_PROXY_SECRET ?>" value="<?= e($proxy_secret) ?>" autocomplete="off">
            <div class="form-text">
                <?= I18N::translate('When set, the API accepts only requests carrying this value in the X-Portal-Proxy-Secret header. Set the same value as a secret on the Cloudflare Worker that serves the portal. Leave empty for local development.') ?>
            </div>
        </div>
    </div>

    <h2 class="h5 mt-4"><?= I18N::translate('Sign-in rate limits') ?></h2>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_RATE_LIMIT_IP ?>">
            <?= I18N::translate('Failed attempts per IP address') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="number" min="0" id="<?= PortalApiModule::SETTING_RATE_LIMIT_IP ?>" name="<?= PortalApiModule::SETTING_RATE_LIMIT_IP ?>" value="<?= e($rate_limit_ip) ?>">
            <div class="form-text"><?= I18N::translate('Zero disables this limit.') ?></div>
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_RATE_LIMIT_USER ?>">
            <?= I18N::translate('Failed attempts per username') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="number" min="0" id="<?= PortalApiModule::SETTING_RATE_LIMIT_USER ?>" name="<?= PortalApiModule::SETTING_RATE_LIMIT_USER ?>" value="<?= e($rate_limit_user) ?>">
            <div class="form-text"><?= I18N::translate('Zero disables this limit.') ?></div>
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_RATE_LIMIT_WINDOW ?>">
            <?= I18N::translate('Time window (seconds)') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="number" min="60" id="<?= PortalApiModule::SETTING_RATE_LIMIT_WINDOW ?>" name="<?= PortalApiModule::SETTING_RATE_LIMIT_WINDOW ?>" value="<?= e($rate_limit_window) ?>">
        </div>
    </div>

    <h2 class="h5 mt-4"><?= I18N::translate('Invitations') ?></h2>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_INVITATION_DAYS ?>">
            <?= I18N::translate('An invitation is valid for (days)') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="number" min="1" max="90" id="<?= PortalApiModule::SETTING_INVITATION_DAYS ?>" name="<?= PortalApiModule::SETTING_INVITATION_DAYS ?>" value="<?= e($invitation_days) ?>">
            <div class="form-text">
                <?= I18N::translate('How long an invitation link keeps working. An unused link stops working after this, and a new one can be issued.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-sm-9 offset-sm-3">
            <?= view('components/checkbox', ['label' => I18N::translate('Members may invite their own close family'), 'name' => PortalApiModule::SETTING_MEMBER_INVITES, 'checked' => $member_invites === '1']) ?>
            <div class="form-text">
                <?= I18N::translate('A member sees a list of their close relatives and can create an invitation link for one of them, without asking you first. You see every invitation on the screen above, and can withdraw one that has not been used.') ?>
            </div>
            <div class="form-text">
                <?= I18N::translate('An invitation creates an account with member access to this family tree, so the two settings below are what keeps it in proportion.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_MEMBER_INVITE_STEPS ?>">
            <?= I18N::translate('How close a relative must be') ?>
        </label>
        <div class="col-sm-9">
            <?= view('components/select', [
                'name'     => PortalApiModule::SETTING_MEMBER_INVITE_STEPS,
                'id'       => PortalApiModule::SETTING_MEMBER_INVITE_STEPS,
                'selected' => $member_invite_steps,
                'options'  => [
                    '1' => I18N::translate('Parents, brothers and sisters, partners and children'),
                    '2' => I18N::translate('Also grandparents, grandchildren, nieces and nephews, parents-in-law'),
                    '3' => I18N::translate('Also first cousins, great-grandparents, great-nieces and great-nephews'),
                ],
            ]) ?>
            <div class="form-text">
                <?= I18N::translate('Measured by this module, by walking the family tree — not by webtrees’ own idea of who is visible, which for a member is usually everybody who is alive.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_MEMBER_INVITE_QUOTA ?>">
            <?= I18N::translate('Invitations one member may have outstanding') ?>
        </label>
        <div class="col-sm-9">
            <input class="form-control" type="number" min="0" max="20" id="<?= PortalApiModule::SETTING_MEMBER_INVITE_QUOTA ?>" name="<?= PortalApiModule::SETTING_MEMBER_INVITE_QUOTA ?>" value="<?= e($member_invite_quota) ?>">
            <div class="form-text">
                <?= I18N::translate('Counts only links that have not been used yet. Zero stops members from inviting anybody, the same as switching it off above.') ?>
            </div>
        </div>
    </div>

    <h2 class="h5 mt-4"><?= I18N::translate('What members can see') ?></h2>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="<?= PortalApiModule::SETTING_MEMBER_PATH_LENGTH ?>">
            <?= I18N::translate('How much of the tree a member sees') ?>
        </label>
        <div class="col-sm-9">
            <?= view('components/select', [
                'name'     => PortalApiModule::SETTING_MEMBER_PATH_LENGTH,
                'id'       => PortalApiModule::SETTING_MEMBER_PATH_LENGTH,
                'selected' => $member_path_length,
                'options'  => [
                    '0' => I18N::translate('No limit — every living person in the tree'),
                    '1' => I18N::translate('Parents, brothers and sisters, partners and children'),
                    '2' => I18N::translate('Also grandparents, grandchildren, nieces and nephews, parents-in-law'),
                    '3' => I18N::translate('Also first cousins, great-grandparents, great-nieces and great-nephews'),
                    '4' => I18N::translate('One step further again'),
                ],
            ]) ?>
            <div class="form-text">
                <?= I18N::translate('This is webtrees’ own per-user “relationship path length”, which this module writes for you. It restricts living people only — everybody who has died stays visible, so the genealogy itself is unaffected.') ?>
            </div>
            <div class="form-text">
                <?= I18N::translate('webtrees leaves this unset by default, which means a member sees every living person in the family tree. New accounts created by invitation get this value; existing accounts keep what they have until you apply it to them on the Diagnosis screen.') ?>
            </div>
            <div class="form-text">
                <?= I18N::translate('Editors, moderators, managers and administrators are never restricted — they need the whole tree to maintain it.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-9">
            <button type="submit" class="btn btn-primary">
                <?= view('icons/save') ?>
                <?= I18N::translate('save') ?>
            </button>
        </div>
    </div>

    <?= csrf_field() ?>
</form>
